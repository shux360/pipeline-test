package com.ticketwave.backend.model.requestSubmission;

public enum RequestStatus {
    REQUEST_PENDING,
    REQUEST_IN_PROGRESS,
    REQUEST_APPROVED,
    REQUEST_REJECTED,
    REQUEST_CANCELLED,
    REQUEST_BYPASSED,
    REQUEST_ESCALATED
}